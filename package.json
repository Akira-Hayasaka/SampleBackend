{
  "name": "backend",
  "version": "1.0.0",
  "description": "sample backend",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon --legacy-watch app.js",    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "akira",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
